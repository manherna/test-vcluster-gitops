apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vcluster.fullname" . }}-test-connection"
  labels:
    {{- include "vcluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vcluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
